import { HighlightActiveDirective } from './highlight-active.directive';

describe('HighlightActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightActiveDirective();
    expect(directive).toBeTruthy();
  });
});
